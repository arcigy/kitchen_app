# Hranipex adapter

Verified read-only against `https://www.hranipex.cz/cs/`. Exact variant IDs are searched through `/cs/vyhledavani/?q=...` and extracted from product variant rows with customer price per metre and stock state. Cart and favorites forms are explicitly outside the adapter.
